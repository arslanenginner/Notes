

React is library note Framework. Library target specific func and framework provide all 
that requried to build a app. HTML in react is jsx mean we can also use js in html.

Descripion and Name etc in react called props and treat as object.